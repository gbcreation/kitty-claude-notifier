# kitty-claude-notifier configuration
# Copied to ~/.config/kitty-claude-notifier/config.toml on install.
# Any field left out here falls back to the built-in default.
#
# Top-level (non-table) keys must stay above the [icons]/[colors] table
# headers below. TOML assigns any key after a [table] header to that
# table, not to the document root.

# Seconds before done/waiting -> idle.
idle_timeout_secs = 300

# How often the resume-detection screen-scrape polls `kitten @ get-text`
# while a session is stuck in permission (milliseconds).
resume_poll_interval_ms = 500

# Text markers (case-insensitive substring match) that indicate a
# permission prompt is still visible on screen. Update this list if
# Claude Code's prompt wording changes across versions.
permission_markers = [
    "do you want to proceed?",
    "❯ 1. yes",
]

# Off by default. When enabled, plays a sound on entering
# Permission/Waiting (needs your input) and on Done (a turn finished).
# Nothing plays for a tab you're already looking at (its focused tab)
# unless sound_play_when_focused is set. Built-in sounds require a
# system audio player to be installed (paplay/pw-play/ffplay/mpg123/mpv
# on Linux, afplay on macOS); silently does nothing if none are found.
sound_enabled = false

# Which states play a sound when entered, on top of sound_enabled above.
# Defaults to all three states that have an associated sound; listing a
# state without one (working/idle/error) has no effect. Restrict to just
# one or two, e.g. sound_events = ["done"] to only hear about turns
# finishing, not permission/waiting prompts.
# sound_events = ["permission", "waiting", "done"]

# Off by default. Set true to play the sound even for the tab you're
# currently focused on (normally suppressed).
sound_play_when_focused = false

# Optional custom sound file paths; omit either to use the built-in
# default (sourced from herdrdev/herdr, see assets/sounds/NOTICE.md).
# [sounds]
# request = "/path/to/custom-request-sound.mp3"
# done = "/path/to/custom-done-sound.mp3"

# Per-state icon overrides: a glyph prepended to the tab's *existing*
# title (whatever the shell already set it to), rendered in the given
# color. Must be a plain Unicode symbol or Nerd Font glyph; emoji carry
# their own embedded color and ignore the `color` setting entirely.
#
# Colors default to white rather than matching the tab-background color
# below: an icon the same color as its own background disappears.
#
# Add a `text` key to prepend the icon onto fixed text instead of the
# tab's live title (the old per-state custom-title behavior, now
# combined with the icon rather than replacing it), e.g.:
#
#   [icons.permission]
#   glyph = "▲"
#   color = "#ffffff"
#   text = "NEEDS APPROVAL"
[icons.working]
glyph = "●"
color = "#ffffff"

[icons.permission]
glyph = "▲"
color = "#ffffff"

[icons.waiting]
glyph = "◐"
color = "#ffffff"

[icons.done]
glyph = "✓"
color = "#ffffff"

[icons.idle]
glyph = "○"
color = "#888888"

[icons.error]
glyph = "✕"
color = "#ffffff"

# Per-state tab color overrides. A plain string applies to both the
# focused and unfocused tab background ("NONE" clears it). To use a
# different color when the tab isn't focused, use a table instead, e.g.:
#
#   [colors.permission]
#   active = "#ff003c"
#   inactive = "#7a0020"
#
# (Kitty only honors `active_bg` while a tab is focused, so without an
# inactive color a background tab wouldn't show any color at all.)
[colors]
working = "#b026ff"
permission = "#ff003c"
waiting = "#00ffd5"
done = "#00ffd5"
idle = "NONE"
error = "#ff6b00"
